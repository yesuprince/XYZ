spring:
  datasource:
    url: jdbc:h2:mem:car_rental_db
    driver-class-name: org.h2.Driver
    username: Test@1234
    password: Test@1234
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080